# ╔═══════════════════════════════════════════════════════════════════╗
# ║  ACMEEH Docker Environment Variables                            ║
# ║  Copy to .env and uncomment/modify as needed.                   ║
# ║  Only POSTGRES_PASSWORD is required.                            ║
# ╚═══════════════════════════════════════════════════════════════════╝

# ── Build Options ─────────────────────────────────────────────────────
# INSTALL_HSM=0
# INSTALL_GEVENT=0
# EXTRA_PIP_PACKAGES=
# ACMEEH_UID=1000
# ACMEEH_GID=1000

# ── PostgreSQL ────────────────────────────────────────────────────────
POSTGRES_PASSWORD=changeme
# POSTGRES_HOST=postgres
# POSTGRES_DB=acmeeh
# POSTGRES_USER=acmeeh
# POSTGRES_SSLMODE=disable

# ── Server ────────────────────────────────────────────────────────────
# ACMEEH_EXTERNAL_URL=https://acmeeh.example.com
# ACMEEH_BIND=0.0.0.0
# ACMEEH_BASE_PATH=

# ── Reverse Proxy ─────────────────────────────────────────────────────
# ACMEEH_FORWARDED_FOR_HEADER=X-Forwarded-For
# ACMEEH_FORWARDED_PROTO_HEADER=X-Forwarded-Proto

# ── CA Backend ────────────────────────────────────────────────────────
# CA_BACKEND=internal
# CA_ROOT_CERT_PATH=/app/certs/root.pem
# CA_ROOT_KEY_PATH=/app/certs/root-key.pem
# CA_KEY_PROVIDER=file
# CA_SERIAL_SOURCE=database
# CA_HASH_ALGORITHM=sha256

# ── External CA ───────────────────────────────────────────────────────
# CA_EXT_SIGN_URL=
# CA_EXT_REVOKE_URL=
# CA_EXT_AUTH_HEADER=Authorization
# CA_EXT_AUTH_VALUE=

# ── ACME Proxy CA ────────────────────────────────────────────────────
# CA_PROXY_DIRECTORY_URL=
# CA_PROXY_EMAIL=
# CA_PROXY_STORAGE_PATH=/app/data/acme_proxy_storage
# CA_PROXY_CHALLENGE_TYPE=dns-01
# CA_PROXY_CHALLENGE_HANDLER=

# ── HSM / PKCS#11 ────────────────────────────────────────────────────
# HSM_PKCS11_LIBRARY=/usr/lib/softhsm/libsofthsm2.so
# HSM_TOKEN_LABEL=acmeeh
# HSM_PIN=
# HSM_KEY_LABEL=acmeeh-signing
# HSM_KEY_TYPE=ec
# HSM_HASH_ALGORITHM=sha256
# HSM_ISSUER_CERT_PATH=/app/certs/issuer.pem
# HSM_SERIAL_SOURCE=database

# ── Logging ───────────────────────────────────────────────────────────
# LOG_LEVEL=INFO
# LOG_FORMAT=human
# AUDIT_LOG_FILE=/var/log/acmeeh/audit.log

# ── SMTP ──────────────────────────────────────────────────────────────
# SMTP_HOST=
# SMTP_USERNAME=
# SMTP_PASSWORD=
# SMTP_FROM=

# ── TOS ───────────────────────────────────────────────────────────────
# TOS_URL=
# TOS_REQUIRE_AGREEMENT=false

# ── Admin API ─────────────────────────────────────────────────────────
# ADMIN_BASE_PATH=/api
# ADMIN_TOKEN_SECRET=
# ADMIN_INITIAL_EMAIL=

# ── Rate Limiting ─────────────────────────────────────────────────────
# RATE_LIMIT_BACKEND=memory

# ── Optional Subsystems ──────────────────────────────────────────────
# CRL_PATH=/crl
# OCSP_PATH=/ocsp
# ARI_PATH=/renewalInfo
# METRICS_PATH=/metrics

# ── Audit Export ──────────────────────────────────────────────────────
# AUDIT_WEBHOOK_URL=
# AUDIT_SYSLOG_HOST=
